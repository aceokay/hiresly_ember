{{yield}}
<div class="container">
  {{#if devLogin}}
  <ul>
    {{#each developers as |developer|}}
    {{#link-to 'developer' developer.id}}
    <button>{{developer.email}}</button>
    {{/link-to}}
    {{/each}}
  </ul>
  {{else}}
    {{#bs-button class="btn-primary center-block" action='devLoginShow'}}Developer Login{{/bs-button}}
  {{/if}}
</div>

<h1>JOSUE TEST STUFF</h1>

<div class="container">
  <form>
    <div class="form-group">
      <label for="email">Email</label>
      {{input value=email id="email" class="form-control" placeholder="example@developer.com"}}
    </div>
    <div class="form-group">
      <label for="password">Password</label>
      {{input type="password" value=password id="password" class="form-control" placeholder="example@developer.com"}}
    </div>
    <button {{action "signIn" email password}}>LOG ME IN</button>
  </form>
</div>







<!-- {{#bs-form}} BREAKING GARBAGE
  {{#bs-form-group}}
    {{#x-select value=developer}}
      {{#each developers as |developer|}}
        {{#x-option value=developer.id}}{{developer.email}}{{/x-option}}
      {{/each}}
    {{/x-select}}
  {{/bs-form-group}}
  <button class="btn btn-success center-block" {{action 'devSelect'}}>Login</button>
{{/bs-form}} -->


<!-- <form> MORE BREAKING GARBAGE
  {{#x-select value=developer}}
    {{#each developers as |developer|}}
      {{#x-option value=developer.id}}{{developer.email}}{{/x-option}}
    {{/each}}
  {{/x-select}}
  <button {{action 'devSelect'}}>Login</button>
</form> -->
