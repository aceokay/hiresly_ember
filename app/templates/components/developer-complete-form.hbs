{{yield}}
<div class="container">
  <h1>Update Your Profile:</h1>
  {{#bs-form}}
    {{#bs-form-group}}
      {{bs-input value=name placeholder="Name"}}
    {{/bs-form-group}}
    <button class=" btn btn-primary" {{action 'completeProfile' developer}}>Update</button>
  {{/bs-form}}
</div>
